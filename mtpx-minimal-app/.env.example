# Conexão com o linkd (escolha um modo)

# 1) TCP (recomendado para host -> Docker, especialmente macOS)
# A autenticação TCP local usa a sessão atual do `mtpx login`
LINKD_CONNECT=tcp://localhost:9999

# Realm usado pelos endpoints HTTP protegidos do exemplo
AUTH_REALM=multpex
AUTH_CLIENT_ID=multpex-services

# LINKD_CONNECT=unix:///tmp/linkd.sock
# LINKD_SOCKET=/tmp/linkd.sock"

NODE_ENV=development
