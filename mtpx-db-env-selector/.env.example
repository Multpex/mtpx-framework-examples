# Database selection (required)
# Format: <provider>-<db-server-type>-<database-name>
# Examples: rds-mysql-voucher, k8s-pg-voucher, local-pg-voucher, docker-mysql-voucher
MULTPEX_DATABASE_NAME=rds-mysql-voucher

# Optional: db server prefix used in keystore credential name
# Credential default name = <MULTPEX_DB_SERVER>:<MULTPEX_DATABASE_NAME>
MULTPEX_DB_SERVER=default

# Optional: explicit keystore credential name override (service_type=database)
# Example: default:rds-mysql-voucher
# MULTPEX_DATABASE_KEYSTORE_NAME=default:rds-mysql-voucher

# Optional: linkd connection
# LINKD_CONNECT=unix:///tmp/linkd.sock
