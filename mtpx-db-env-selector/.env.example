# Conexão com o linkd (TCP via Docker)
LINKD_CONNECT=tcp://localhost:9999
LINKD_TCP_TOKEN=<gerado pelo install.sh>

# Database selection (required)
# Format: <provider>-<db-server-type>-<database-name>
# Example: docker-pg-test
LINKD_DATABASE_NAME=docker-pg-test

NODE_ENV=development
