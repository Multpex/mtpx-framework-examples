{
  "name": "mtpx-fluent-v2-lambda",
  "version": "0.1.6",
  "type": "module",
  "description": "Example focused on Fluent V2 lambda-style where queries and the explicit predicate API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@linkd/sdk-typescript": "0.1.5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
