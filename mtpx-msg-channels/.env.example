# Conexão com o linkd (TCP via Docker)
LINKD_CONNECT=tcp://localhost:9999
LINKD_TCP_TOKEN=<gerado pelo install.sh>

# Namespace dos serviços de mensageria
LINKD_NAMESPACE=mtpx-msg-channels

# DEBUG=true
NODE_ENV=development
